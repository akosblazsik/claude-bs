Delegate this task to the @orchestrator agent.

**Task:** $ARGUMENTS

The orchestrator will:
1. Classify the task (architecture / integration / implementation / review).
2. Route to the appropriate specialist(s).
3. Require review for non-trivial implementations.
4. Synthesize results into one coherent answer.

This is the default entry point for substantial work.
